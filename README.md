# Clipboard APP
A Flutter application that syncs the clipboard between Windows and Android using Firebase REST API.

## Setup
1. Run `flutter pub get` to install dependencies.
2. Run on Windows: `flutter run -d windows`
3. Run on Android: `flutter run -d <your-device-id>`
